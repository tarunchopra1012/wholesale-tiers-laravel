import { Card, Page, Text } from '@shopify/polaris';

// Placeholder so the route exists. The price preview is its own session.
export default function Preview() {
    return (
        <Page title="Price preview">
            <Card>
                <Text as="p">Not built yet.</Text>
            </Card>
        </Page>
    );
}
