import { Card, Page, Text } from '@shopify/polaris';

// Placeholder so the route exists. The tier form is its own session.
export default function Settings() {
    return (
        <Page title="Settings">
            <Card>
                <Text as="p">Not built yet.</Text>
            </Card>
        </Page>
    );
}
